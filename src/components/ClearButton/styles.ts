import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  clearButtonContainer: {
    width: '30%',
    height: 25,
    borderWidth: 1,
    borderColor: 'red',
    margin: 15,
  },
  btnText: {
    textAlign: 'center',
    color: 'red',
  },
});
